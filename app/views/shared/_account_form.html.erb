<%= form_for(@account) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.label :account_name %>
    <%= f.text_field :name, placeholder: "Create new account..." %>
  </div>
  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>